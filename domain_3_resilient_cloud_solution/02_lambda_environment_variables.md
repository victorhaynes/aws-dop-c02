# AWS Lambda Environment Variables
- Key value pair in a string form
- Lambda provides its own ssytem env vars again
- these can be encrypted (by KMS for example)
- can be encrypted by the Lambda service key or a CMK