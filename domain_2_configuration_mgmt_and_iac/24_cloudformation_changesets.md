# ChangeSets
- When you update a stack you need to know what changes will happen before it applies for greate confidence
- Won't say if it will pass or fail but it is like `terraform plan` for StackSets
