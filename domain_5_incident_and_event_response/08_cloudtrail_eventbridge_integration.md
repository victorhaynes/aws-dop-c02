# CloudTrail EventBridge Integration
- Ex. receive a SNS notificaiton if someone deletes a DynamodB table

- The API Call gets put in CloudTrail by default
- Also ends up in EB
- From EB we can setup an alert