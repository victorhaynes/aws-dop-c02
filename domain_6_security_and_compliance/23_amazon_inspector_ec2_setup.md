# Amazon Inspector EC2 Setup
- Need to install and enabled the SSM agent
- must bean ASSM Managed Instance (IAM Role or Default Host Management Config)
- requires outbound 443 to systems manager edpoint

this is default on some AMIs